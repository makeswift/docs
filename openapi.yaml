openapi: 3.1.0
paths:
  "/v2/locales":
    post:
      operationId: LocaleController_create_v2
      summary: "Create Locale"
      security:
        - Authentication: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLocaleV2RequestBodyDto"
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateLocaleV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Locale
    get:
      operationId: LocaleController_list_v2
      summary: "List Locales"
      description: "List locales in a site"
      security:
        - Authentication: []
      parameters:
        - name: siteId
          required: true
          in: query
          schema:
            format: uuid
            type: string
        - name: limit
          required: false
          in: query
          description: "Maximum number of locales to return. Defaults to 20, maximum is 100. To paginate, use together with `after` cursor."
          schema:
            minimum: 0
            maximum: 100
            default: 20
            type: number
        - name: after
          required: false
          in: query
          description: "Cursor for pagination. If provided, returns locales after this ID."
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListLocaleV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Locale
  "/v2/locales/{localeIdOrCode}":
    get:
      operationId: LocaleController_get_v2
      summary: "Get Locale"
      security:
        - Authentication: []
      parameters:
        - name: localeIdOrCode
          required: true
          in: path
          description: The locale ID (UUID) or locale code (e.g., es-MX).
          schema:
            type: string
        - name: siteId
          required: false
          in: query
          description: The site ID (UUID). Required when using a locale code instead of a locale ID.
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadLocaleV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Locale
  "/v2/locales/{localeId}":
    patch:
      operationId: LocaleController_update_v2
      summary: "Update Locale"
      security:
        - Authentication: []
      parameters:
        - name: localeId
          required: true
          in: path
          description: The locale ID (UUID).
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLocaleV2RequestBodyDto"
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateLocaleV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Locale
    delete:
      operationId: LocaleController_delete_v2
      summary: "Delete Locale"
      security:
        - Authentication: []
      parameters:
        - name: localeId
          required: true
          in: path
          description: The locale ID (UUID).
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: ""
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Locale
  "/v2/sites":
    post:
      operationId: SiteController_create_v2
      summary: "Create Site"
      security:
        - Authentication: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSiteV2RequestBodyDto"
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSiteV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Site
    get:
      operationId: SiteController_list_v2
      summary: "List Sites"
      description: "List sites in a workspace"
      security:
        - Authentication: []
      parameters:
        - name: workspaceId
          required: true
          in: query
          schema:
            format: uuid
            type: string
        - name: limit
          required: false
          in: query
          schema:
            minimum: 0
            maximum: 100
            default: 20
            type: number
        - name: start
          required: false
          in: query
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSiteV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Site
  "/v2/sites/{siteId}":
    get:
      operationId: SiteController_getOne_v2
      summary: "Get Site"
      description: "Get a site by its ID"
      security:
        - Authentication: []
      parameters:
        - name: siteId
          required: true
          in: path
          schema:
            format: uuid
            type: string
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadSiteV1ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Site
    patch:
      operationId: SiteController_updateOne_v2
      summary: "Update Site"
      security:
        - Authentication: []
      parameters:
        - name: siteId
          required: true
          in: path
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSiteV1RequestBodyDto"
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateSiteV1ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Site
    delete:
      operationId: SiteController_delete_v2
      summary: "Delete Site"
      security:
        - Authentication: []
      parameters:
        - name: siteId
          required: true
          in: path
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: ""
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Site
  "/v2/sites/{siteId}/duplicate":
    post:
      operationId: SiteController_duplicate_v2
      summary: "Duplicate Site"
      security:
        - Authentication: []
      parameters:
        - name: siteId
          required: true
          in: path
          description: The ID of the site to duplicate.
          schema:
            format: uuid
            example: 123e4567-e89b-12d3-a456-426614174000
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DuplicateSiteV1RequestBodyDto"
      responses:
        "201":
          description: The newly created duplicated site with all content and configuration copied from the original.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DuplicateSiteV1ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Site
  "/v2/pages":
    get:
      operationId: PageController_list_v2
      summary: "List Pages"
      description: "List pages in a site"
      security:
        - Authentication: []
      parameters:
        - name: siteId
          required: true
          in: query
          schema:
            format: uuid
            type: string
        - name: limit
          required: false
          in: query
          description: "Maximum number of pages to return. Defaults to 20, maximum is 100."
          schema:
            minimum: 1
            maximum: 100
            default: 20
            type: number
        - name: after
          required: false
          in: query
          description: "Cursor for pagination. If provided, returns pages after this ID."
          schema:
            format: uuid
            type: string
        - name: pathPrefix
          required: false
          in: query
          description: "A filter to only retrieve pages with a pathname that begins with this value."
          schema:
            type: string
            default: "/"
        - name: sortBy
          required: false
          in: query
          description: "The field to sort the result pages by."
          schema:
            type: string
            enum:
              - title
              - path
              - description
              - createdAt
              - updatedAt
            default: path
        - name: sortDirection
          required: false
          in: query
          description: "The direction of how the result pages are sorted."
          schema:
            type: string
            enum:
              - asc
              - desc
            default: asc
        - name: includeOffline
          required: false
          in: query
          description: "Whether the results should include pages that are offline in Makeswift."
          schema:
            type: boolean
            default: false
        - name: locale
          required: false
          in: query
          description: "A valid locale string (ex. 'en-US')."
          schema:
            type: string
        - name: version
          required: false
          in: query
          description: "The version to retrieve. Can be 'live', 'draft', or a site version UUID."
          schema:
            type: string
            default: live
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListPagesV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Page
    post:
      operationId: PageController_create_v1
      summary: "Create Page"
      description: "Create a new page in a site"
      x-mint:
        metadata:
          title: "Create Page"
      security:
        - Authentication: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePageV1RequestBodyDto"
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreatePageV1ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Page
  "/v2/pages/{pageIdOrPathname}":
    get:
      operationId: PageController_getOne_v1
      summary: "Get Page"
      security:
        - Authentication: []
      parameters:
        - name: pageIdOrPathname
          required: true
          in: path
          description: The page ID (UUID) or pathname of the page.
          schema:
            type: string
        - name: siteId
          required: false
          in: query
          description: The site ID (UUID). Required when using a pathname instead of a page ID.
          schema:
            format: uuid
            type: string
        - name: version
          required: false
          in: query
          description: "The version to retrieve. Can be 'live', 'draft', or a site version UUID."
          schema:
            type: string
            default: live
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetPageV1ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Page
  "/v2/pages/{pageId}":
    patch:
      operationId: PageController_update_v1
      summary: "Update Page"
      security:
        - Authentication: []
      parameters:
        - name: pageId
          required: true
          in: path
          description: The page ID (UUID).
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePageV1RequestBodyDto"
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdatePageV1ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Page
    delete:
      operationId: PageController_delete_v1
      summary: "Delete Page"
      security:
        - Authentication: []
      parameters:
        - name: pageId
          required: true
          in: path
          description: The page ID (UUID).
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: "No Content - The page was successfully deleted."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Page
  "/v2/routes":
    get:
      operationId: RouteController_list_v2
      summary: "List Routes"
      description: "List routes in a site"
      security:
        - Authentication: []
      parameters:
        - name: siteId
          required: true
          in: query
          schema:
            format: uuid
            type: string
        - name: pathname
          required: false
          in: query
          description: "Filter routes by pathname. Supports partial match. If not provided, returns all routes."
          schema:
            example: /furniture
            type: string
        - name: limit
          required: false
          in: query
          description: "Maximum number of routes to return. Defaults to 20, maximum is 100. To paginate, use together with `after` cursor."
          schema:
            minimum: 0
            maximum: 100
            default: 20
            type: number
        - name: after
          required: false
          in: query
          description: "Cursor for pagination. If provided, returns routes after this ID."
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRouteV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Route
    post:
      operationId: RouteController_create_v2
      summary: "Create Route"
      security:
        - Authentication: []
      parameters:
        - name: skipValidation
          required: false
          in: query
          description: "If true, skips validation of the route, such as pathname format. This is useful for testing or when the route is known to be valid."
          schema:
            enum:
              - "true"
              - "false"
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRouteV2RequestBodyDto"
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateRouteV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Route
  "/v2/routes/{routeId}":
    get:
      operationId: RouteController_get_v2
      summary: "Get Route"
      security:
        - Authentication: []
      parameters:
        - name: routeId
          required: true
          in: path
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadRouteV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Route
    patch:
      operationId: RouteController_update_v2
      summary: "Update Route"
      security:
        - Authentication: []
      parameters:
        - name: routeId
          required: true
          in: path
          schema:
            format: uuid
            type: string
        - name: skipValidation
          required: false
          in: query
          description: "If true, skips validation of the route, such as pathname format. This is useful for testing or when the route is known to be valid."
          schema:
            enum:
              - "true"
              - "false"
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRouteV2RequestBodyDto"
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateRouteV2ResponseBodyDto"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Route
    delete:
      operationId: RouteController_delete_v2
      summary: "Delete Route"
      security:
        - Authentication: []
      parameters:
        - name: routeId
          required: true
          in: path
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: ""
        "401":
          $ref: "#/components/responses/UnauthorizedError"
      tags:
        - Route
info:
  title: Hearth Furniture API
  description: Documentation for the Hearth Furniture Company public REST API.
  version: "1.0"
  contact: {}
tags:
  - name: Site
    description: ""
  - name: Page
    description: ""
  - name: Locale
    description: ""
  - name: Route
    description: ""
servers:
  - url: "https://8511e6e5e351.ngrok-free.app"
    description: "Local development server with ngrok"
  - url: "http://localhost:8020"
    description: "Local development server"
components:
  securitySchemes:
    Authentication:
      type: http
      scheme: bearer
      description: "The API key for the App (e.g. sk_eczMHVBY9fV6GYFhvs53qcnxq7yptlXL3ABKOZtn6dQ)"

  responses:
    UnauthorizedError:
      description: Access token is missing or invalid
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: string
                example: "Unauthorized"
              message:
                type: string
                example: "Unauthorized"
          example:
            error: "Unauthorized"
            message: "Unauthorized"

  schemas:
    CreateLocaleV2RequestBodyDto:
      type: object
      properties:
        siteId:
          type: string
          format: uuid
          description: The ID of the site.
        locale:
          type: string
          description: Locale code (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
          example: es-MX
        domain:
          type: string
          nullable: true
          format: uri
          description: Domain for domain-based localization. Domain URL must include the protocol.
          example: "https://hearthfurniture.com.mx"
      required:
        - siteId
        - locale
    CreateLocaleV2ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        locale:
          type: string
          description: Locale code (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
          example: es-MX
        domain:
          type: string
          nullable: true
          format: uri
          description: Domain for domain-based localization. Domain URL must include the protocol.
          example: "https://hearthfurniture.com.mx"
    UpdateLocaleV2RequestBodyDto:
      type: object
      properties:
        locale:
          type: string
          description: Locale code (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
          example: es-MX
        domain:
          type: string
          nullable: true
          format: uri
          description: Domain for domain-based localization. Domain URL must include the protocol.
          example: "https://hearthfurniture.com.mx"
    UpdateLocaleV2ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        locale:
          type: string
          description: Locale code (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
          example: es-MX
        domain:
          type: string
          nullable: true
          format: uri
          description: Domain for domain-based localization. Domain URL must include the protocol.
          example: "https://hearthfurniture.com.mx"
    ReadLocaleV2ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        locale:
          type: string
          description: Locale code (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
          example: es-MX
        domain:
          type: string
          nullable: true
          format: uri
          description: Domain for domain-based localization. Domain URL must include the protocol.
          example: "https://hearthfurniture.com.mx"
    ListLocaleV2ResponseBodyDto:
      type: object
      properties:
        data:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              locale:
                type: string
                description: Locale code (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
                example: es-MX
              domain:
                type: string
                nullable: true
                format: uri
                description: Domain for domain-based localization. Domain URL must include the protocol.
                example: "https://hearthfurniture.com.mx"
        hasMore:
          type: boolean
          description: Whether there are more locales available for pagination.
    CreateSiteV2RequestBodyDto:
      type: object
      properties:
        workspaceId:
          type: string
          description: The ID of the workspace this site belongs to.
          format: uuid
        name:
          type: string
          description: The name of the site.
          example: Hearth Furniture Company
        hostUrl:
          type: string
          nullable: true
          format: uri
          description: The host URL of the site. The URL should consist of only the origin.
          example: "https://hearthfurniture.com"
        defaultLocale:
          type: string
          description: The primary locale of the site (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
          example: en-US
          x-mint:
            hideInExample: true
      required:
        - name
    CreateSiteV2ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: The name of the site.
          example: Hearth Furniture Company
        hostUrl:
          type: string
          nullable: true
          format: uri
          description: The host URL of the site. The URL should consist of only the origin.
          example: "https://hearthfurniture.com"
        apiKey:
          type: string
          format: uuid
        defaultLocale:
          type: string
          description: The primary locale of the site (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
          example: en-US
    ReadSiteV1ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: The name of the site.
          example: Hearth Furniture Company
        hostUrl:
          type: string
          nullable: true
          format: uri
          description: The host URL of the site. The URL should consist of only the origin.
          example: "https://hearthfurniture.com"
        apiKey:
          type: string
          format: uuid
        defaultLocale:
          type: string
          description: The primary locale of the site (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
          example: en-US
    UpdateSiteV1RequestBodyDto:
      type: object
      properties:
        name:
          type: string
          description: The name of the site.
          example: Hearth Furniture Company
        hostUrl:
          type: string
          nullable: true
          format: uri
          description: The host URL of the site. The URL should consist of only the origin.
          example: "https://hearthfurniture.com"
    UpdateSiteV1ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: The name of the site.
          example: Hearth Furniture Company
        hostUrl:
          type: string
          nullable: true
          format: uri
          description: The host URL of the site. The URL should consist of only the origin.
          example: "https://hearthfurniture.com"
        apiKey:
          type: string
          format: uuid
    ListSiteV2ResponseBodyDto:
      type: object
      properties:
        data:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              name:
                type: string
                description: The name of the site.
                example: Hearth Furniture Company
              hostUrl:
                type: string
                nullable: true
                format: uri
                description: The host URL of the site. The URL should consist of only the origin.
                example: "https://hearthfurniture.com"
              apiKey:
                type: string
                format: uuid
              defaultLocale:
                type: string
                description: The primary locale of the site (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
                example: en-US
        hasMore:
          type: boolean
    DuplicateSiteV1RequestBodyDto:
      type: object
      properties:
        name:
          type: string
          description: The name for the new duplicated site.
          example: Hearth Furniture Duplicate
    DuplicateSiteV1ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: The name of the site.
          example: Hearth Furniture Company
        hostUrl:
          type: string
          nullable: true
          format: uri
          description: The host URL of the site. The URL should consist of only the origin.
          example: "https://hearthfurniture.com"
        apiKey:
          type: string
          format: uuid
        defaultLocale:
          type: string
          description: The primary locale of the site (e.g., es-MX for Mexican Spanish, fr-CA for Canadian French).
          example: en-US
      description: The newly created duplicated site with all content and configuration copied from the original.
    CreatePageV1RequestBodyDto:
      type: object
      properties:
        siteId:
          type: string
          format: uuid
          description: The ID of the site.
        pathname:
          type: string
          description: The pathname of the page.
        name:
          type: string
          description: The name of the page.
      required:
        - siteId
        - pathname
        - name
    PageV2Dto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        pathname:
          type: string
          description: The pathname of the page.
          example: furniture
        canonicalUrl:
          type: string
          nullable: true
          description: The canonical URL of the page.
        title:
          type: string
          nullable: true
          description: The title of the page.
        description:
          type: string
          nullable: true
          description: The description of the page.
        socialImageUrl:
          type: string
          nullable: true
          description: The social image URL of the page.
        sitemapPriority:
          type: number
          nullable: true
          minimum: 0
          maximum: 1
          description: The priority of the page in the sitemap.
        sitemapFrequency:
          type: string
          nullable: true
          enum:
            - always
            - hourly
            - daily
            - weekly
            - monthly
            - yearly
            - never
          description: The frequency with which the page is updated in the sitemap.
        createdAt:
          type: string
          format: date-time
          nullable: true
          description: The date and time when the page was created.
        updatedAt:
          type: string
          format: date-time
          nullable: true
          description: The date and time when the page was last updated.
        publishedAt:
          type: string
          format: date-time
          nullable: true
          description: The date and time when the page was published.
        isOnline:
          type: boolean
          description: Whether the page is online.
        excludedFromSearch:
          type: boolean
          nullable: true
          description: Whether the page is excluded from search.
        locale:
          type: string
          description: The locale of the page.
        localizations:
          type: array
          description: The localized variants of the page.
          items:
            type: object
            properties:
              locale:
                type: string
                description: The locale of the localized variant.
              pathname:
                type: string
                description: The pathname of the localized variant.
              id:
                type: string
                format: uuid
                description: The ID of the localized variant.
    CreatePageV1ResponseBodyDto:
      $ref: "#/components/schemas/PageV2Dto"
    UpdatePageV1RequestBodyDto:
      type: object
      properties:
        pathname:
          type: string
          description: The pathname of the page.
        name:
          type: string
          description: The name of the page.
        isOnline:
          type: boolean
          description: Whether the page is online or offline.
    UpdatePageV1ResponseBodyDto:
      $ref: "#/components/schemas/PageV2Dto"
    GetPageV1ResponseBodyDto:
      $ref: "#/components/schemas/PageV2Dto"
    ListPagesV2ResponseBodyDto:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/PageV2Dto"
        hasMore:
          type: boolean
          description: Whether there are more pages available for pagination.
    CreateRouteV2RequestBodyDto:
      type: object
      properties:
        siteId:
          type: string
          format: uuid
          description: The ID of the site.
        pathname:
          type: string
          description: 'Pathname of the route, e.g., "/about".'
          example: /furniture
        localizations:
          default: []
          type: array
          items:
            type: object
            properties:
              locale:
                type: string
                description: Locale code.
                example: es-MX
              pathname:
                type: string
                description: 'Pathname of the route, e.g., "/about".'
                example: /furniture
            required:
              - locale
              - pathname
            description: Localized variant of a route. Each variant has a locale and a pathname.
            example:
              locale: es-ES
              pathname: /sobre-nosotros
          description: "List of localized variants for the route. If not provided, defaults to an empty array."
          example:
            - locale: es-ES
              pathname: /sobre-nosotros
            - locale: fr-FR
              pathname: /a-propos
      required:
        - siteId
        - pathname
      description: Request body for creating a new route.
    CreateRouteV2ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        pathname:
          type: string
          description: 'Pathname of the route, e.g., "/about".'
          example: /furniture
        localizations:
          type: array
          items:
            type: object
            properties:
              locale:
                type: string
                description: Locale code.
                example: es-MX
              pathname:
                type: string
                description: 'Pathname of the route, e.g., "/about".'
                example: /furniture
            required:
              - locale
              - pathname
            description: Localized variant of a route. Each variant has a locale and a pathname.
            example:
              locale: es-ES
              pathname: /sobre-nosotros
          description: List of localized variants for the route. Each variant has a locale and a pathname.
          example:
            - locale: es-ES
              pathname: /sobre-nosotros
            - locale: fr-FR
              pathname: /a-propos
      description: Route with its localized variants.
      example:
        id: 37b18c41-9752-4adf-9aef-f42ab4f74b04
        pathname: /about
        localizations:
          - locale: es-MX
            pathname: /muebles
          - locale: fr-CA
            pathname: /meubles
    ReadRouteV2ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        pathname:
          type: string
          description: 'Pathname of the route, e.g., "/about".'
          example: /furniture
        localizations:
          type: array
          items:
            type: object
            properties:
              locale:
                type: string
                description: Locale code.
                example: es-MX
              pathname:
                type: string
                description: 'Pathname of the route, e.g., "/about".'
                example: /furniture
            required:
              - locale
              - pathname
            description: Localized variant of a route. Each variant has a locale and a pathname.
            example:
              locale: es-ES
              pathname: /sobre-nosotros
          description: List of localized variants for the route. Each variant has a locale and a pathname.
          example:
            - locale: es-ES
              pathname: /sobre-nosotros
            - locale: fr-FR
              pathname: /a-propos
      required:
        - id
        - pathname
        - localizations
      description: Route with its localized variants.
      example:
        id: 37b18c41-9752-4adf-9aef-f42ab4f74b04
        pathname: /about
        localizations:
          - locale: es-MX
            pathname: /muebles
          - locale: fr-CA
            pathname: /meubles
    UpdateRouteV2RequestBodyDto:
      type: object
      properties:
        pathname:
          type: string
          description: 'Pathname of the route, e.g., "/about".'
          example: /furniture
        localizations:
          type: array
          items:
            type: object
            properties:
              locale:
                type: string
                description: Locale code.
                example: es-MX
              pathname:
                type: string
                description: 'Pathname of the route, e.g., "/about".'
                example: /furniture
            required:
              - locale
              - pathname
            description: Localized variant of a route. Each variant has a locale and a pathname.
            example:
              locale: es-ES
              pathname: /sobre-nosotros
          description: List of localized variants for the route. Each variant has a locale and a pathname.
          example:
            - locale: es-ES
              pathname: /sobre-nosotros
            - locale: fr-FR
              pathname: /a-propos
      description: Request body for updating an existing route.
    UpdateRouteV2ResponseBodyDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        pathname:
          type: string
          description: 'Pathname of the route, e.g., "/about".'
          example: /furniture
        localizations:
          type: array
          items:
            type: object
            properties:
              locale:
                type: string
                description: Locale code.
                example: es-MX
              pathname:
                type: string
                description: 'Pathname of the route, e.g., "/about".'
                example: /furniture
            description: Localized variant of a route. Each variant has a locale and a pathname.
            example:
              locale: es-ES
              pathname: /sobre-nosotros
          description: List of localized variants for the route. Each variant has a locale and a pathname.
          example:
            - locale: es-ES
              pathname: /sobre-nosotros
            - locale: fr-FR
              pathname: /a-propos
      description: Route with its localized variants.
      example:
        id: 37b18c41-9752-4adf-9aef-f42ab4f74b04
        pathname: /about
        localizations:
          - locale: es-MX
            pathname: /muebles
          - locale: fr-CA
            pathname: /meubles
    ListRouteV2ResponseBodyDto:
      type: object
      properties:
        data:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              pathname:
                type: string
                description: 'Pathname of the route, e.g., "/about".'
                example: /furniture
              localizations:
                type: array
                items:
                  type: object
                  properties:
                    locale:
                      type: string
                      description: Locale code.
                      example: es-MX
                    pathname:
                      type: string
                      description: 'Pathname of the route, e.g., "/about".'
                      example: /furniture
                  description: Localized variant of a route. Each variant has a locale and a pathname.
                  example:
                    locale: es-ES
                    pathname: /sobre-nosotros
                description: List of localized variants for the route. Each variant has a locale and a pathname.
                example:
                  - locale: es-ES
                    pathname: /sobre-nosotros
                  - locale: fr-FR
                    pathname: /a-propos
            description: Route with its localized variants.
            example:
              id: 37b18c41-9752-4adf-9aef-f42ab4f74b04
              pathname: /about
              localizations:
                - locale: es-MX
                  pathname: /muebles
                - locale: fr-CA
                  pathname: /meubles
        hasMore:
          type: boolean
